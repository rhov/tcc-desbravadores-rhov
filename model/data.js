
// Dados em memória
// Clube: { id, nome, unidades: [String] }
// Desbravador: { nome, idade, documento, clubeNome, unidade }
const clubes = [];
const desbravadores = [];
const users = [];

module.exports = {
  clubes,
  desbravadores,
  users,
};
