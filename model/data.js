
// Dados em memória
// Clube: { id, nome, unidades: [{ id, nome, sexo }] }
// Desbravador: { id, nome, idade, documento, sexo, clubeId, unidadeId }
const clubes = [];
const desbravadores = [];
const users = [];

module.exports = {
  clubes,
  desbravadores,
  users,
};
